html
    head
    style
        include style.css
    body
        h2.acch2 Zaloguj się
        form.accform(action="/login" method="post")
            input(type="hidden" name="_csrf" value=csrftoken)
            label() login
            input#username(type="text" name="username" maxlength="40" size="25" required)
            label() password
            input(type="password" name="password" maxlength="40" size="25" required)
            button.accbutton(type="submit") submit
        if warning
            p= "failed login"
